import React from 'react';
import '../css/skeleton-project-image.css';

const PlaceholderImage = () => {
    return (
        <div className="skeleton-img p-3"
        ></div>
    );
};

export default PlaceholderImage;
